import io;

AddMultConstants {

    .method public static main(args.array.String).V {

        b.i32 :=.i32 1.i32;

        t1.bool :=.bool 1.i32 <.bool 2.i32;

        if (t1.bool) goto if1;

            b.i32 :=.i32 2.i32;

        goto endif1;

        if1:

            a.AddMultConstants :=.AddMultConstants new(AddMultConstants).AddMultConstants;
            invokespecial(a.AddMultConstants, "<init>").V;

        endif1:

        ret.V;

    }

    .construct AddMultConstants().V {
        invokespecial(this, "<init>").V;
    }
}