import ioPlus;

SymbolTable {

    .method public static main(args.array.String).V {

        t1.i32 :=.i32 2.i32 /.i32 2.i32;
        result.i32 :=.i32 5.i32 -.i32 t1.i32;

        invokestatic(ioPlus, "printResult", result.i32).V;

        ret.V;

    }

    .construct SymbolTable().V {
        invokespecial(this, "<init>").V;
    }
}